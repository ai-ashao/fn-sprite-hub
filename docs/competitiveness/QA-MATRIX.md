# 本轮 QA 状态映射

日期：2026-09-11。这里区分 **模块逻辑已测** 与 **真实产品流程已测**。测试脚本存在不算通过。

## 目标仓库集成复验

- `pnpm verify` 完整通过：25 个 Vitest 文件共 163 项测试、client／SSR 生产构建、严格类型检查、服务端 smoke，以及 3 个 Playwright 文件共 35 项浏览器测试全部通过。
- 新增竞争力浏览器用例覆盖损坏／异季／未知收藏保护、storage 异常、原生 Web Locks 双标签协调、恢复确认与冲突、筛选上下文返回、分享取消与 PNG 下载、Crown／Klombo 来源、SSR／404／noindex。
- 这仍不代表 Safari／iPhone、真实系统分享面板、生产部署或人工视觉复评已经完成。

## 增量包生成环境的原始运行记录

- `scripts/verify-core.mjs`：35 个测试体在 Node 的 `node:test` 下运行，通过 35，失败 0。使用实际新增存储、状态、上下文、发布清单与分享出口判断模块；将测试框架入口从 Vitest 映射到 node:test，不替换业务代码。
- 同一测试文件 `tests/fn-sprite-competitiveness.test.ts` 在包生成环境未运行真正的 Vitest 全套；目标仓库复验已经覆盖。
- 5 个纯 TypeScript 模块在 TypeScript 5.8.3 严格模式下通过；项目指定 TypeScript 5.9.2 的全量 `pnpm typecheck` 未运行。
- 新增 TS／TSX 与部分已校验基线的变更进行了语法解析。语法检查不等于模块解析、React 运行或生产构建。
- Chromium 原生浏览器核心测试尝试了 16 个场景，全部遇到 `ERR_BLOCKED_BY_ADMINISTRATOR`；没有任何浏览器通过记录，不把这些归为产品断言失败。
- 包生成环境没有完成完整站点与生产构建；目标仓库已完成 Chromium 自动化与生产构建，Safari／iPhone 实机仍未验证。

## 33 项计划场景的包生成时状态

| 计划 ID | 覆盖情况 | 本轮判定 |
|---|---|---|
| QA01 | 首次打开不写空收藏 | 模块通过；页面端待执行 |
| QA02 | 旧 v1 无新字段恢复 | 模块通过；页面端待执行 |
| QA03 | 损坏原始值保持 | 模块通过；浏览器阻塞 |
| QA04 | 异季原始值保持 | 模块通过；浏览器阻塞 |
| QA05 | 未知 schema／字段保持 | 模块通过；浏览器阻塞 |
| QA06 | storage getter 抛错 | 模拟模块通过；页面端待执行 |
| QA07 | setItem 失败、可导出内存 | 模拟模块通过；页面端待执行 |
| QA08 | 双客户端不同 Entry 顺序更新 | 模块通过；原生双标签阻塞 |
| QA09 | 过期客户端操作读取最新值 | 模块通过；焦点事件集成待执行 |
| QA10 | 外部同步无重复写回 | 模块通过；原生事件阻塞 |
| QA11 | 接近同时修改 | 注入互斥队列压力测试通过；原生 Web Locks 阻塞，不能声称实机验证 |
| QA12 | 撤销保留其他 Entry 修改 | 模块通过；页面端待执行 |
| QA13 | 同 Entry 新修改／ABA 撤销冲突 | 模块通过；页面端待执行 |
| QA14 | 恢复预览取消不修改 | 模块预览无写入通过；实际取消按钮待执行 |
| QA15 | 非法／异季／未知 ID／文件错误 | 数据校验模块通过；文件大小／读取失败 UI 待执行 |
| QA16 | 确认过期重新预览 | 模块通过；实际双标签预览待执行 |
| QA17 | 保护或写入失败不替换 | 模块通过；浏览器阻塞 |
| QA18 | 恢复并重开、原始字节与未保存状态保护 | 模块通过；实际下载／刷新待执行 |
| QA19 | Missing → 详情 → 返回 | 上下文校验与读写模块通过；完整流程待执行 |
| QA20 | 详情直达对应家族 | 参数优先级模块通过；滚动位置待执行 |
| QA21 | 已移出 Missing 保留筛选 | 产品用例已写；待执行 |
| QA22 | 无效参数与失效偏好 | 模块通过；实际路由待执行 |
| QA23 | 快速切模板 | 出口 key 判断模块通过；异步 Renderer 集成待执行 |
| QA24 | busy／关闭／失败后不导出旧结果 | 出口模块通过；完整状态机与 UI 待执行 |
| QA25 | 原生取消／不支持 | 代码已处理；真实原生分享未验证 |
| QA26 | 多页 PNG／资源失败 | 每页文件校验的浏览器测试已写；待执行 |
| QA27 | Crown／Klombo 内容 | 官方资料已阅读、来源与补丁范围已记录；页面截图未验收 |
| QA28 | 未确认变体不自动计入 | 显式清单模块通过；保留现有目录，不宣称重新核实全部发布状态 |
| QA29 | SSR／canonical／404 | 产品浏览器测试已写；未运行，不能称通过 |
| QA30 | noindex／隔离未来发布 | 应用器保持原文件、两项隔离测试已写；完整运行待执行 |
| QA31 | 移动菜单、筛选、Matrix | 保留原覆盖并增加返回路径用例；本轮未实测 |
| QA32 | 键盘／弹窗焦点／降低动态效果 | 没有完成新的人工验收；待验证 |
| QA33 | 动态分母 | 新模块用 3／4 条目录测试通过，改动了两个原测试文件；全项目其余历史夹具仍需复查 |

## 重点本地补测

优先运行新增浏览器文件，检查数据保护、双标签与恢复确认，再运行全部 `pnpm verify`。随后检查桌面首页、Crown／Klombo、Mastery，及 375／390 宽移动端的筛选返回、Matrix、保存异常和分享对话框。

性能报告、前后资源体积、完整人工截图、浏览器支持矩阵都不能从本轮纯逻辑测试推导。本轮不更新产品总分。
